# - Config file for the cfstlookup package
# It defines the following variables
#  CFSTLOOKUP_INCLUDE_DIRS - include directories for cfstlookup
#  CFSTLOOKUP_LIBRARIES    - libraries to link against

# Compute paths
get_filename_component(CFSTLOOKUP_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
set(CFSTLOOKUP_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")

# Our library dependencies (contains definitions for IMPORTED targets)
if(NOT TARGET cfstlookup AND NOT cfstlookup_BINARY_DIR)
  include("${CFSTLOOKUP_CMAKE_DIR}/cfstlookup-targets.cmake")
endif()

# These are IMPORTED targets created by cfstlookupTargets.cmake
set(CFSTLOOKUP_LIBRARIES cfstlookup)
